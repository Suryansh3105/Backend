services:
  db:
    image: postgres:17.4
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: Hello123
      POSTGRES_DB: authDB
      POSTGRES_USER: post
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
